using UnityEngine;

public class AutoSimulation_main1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Test");


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
